// Topic: Browsing standard library documentation

// `rustup doc` 명령어를 사용하여 표준 라이브러리 문서 열기

fn main() {
    let my_str = "this is my string";
    pritln!("uppercase: {}", my_str.to_uppercase());
    pritln!("lowercase: {}", my_str.to_lowercase());
}
